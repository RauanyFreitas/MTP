#include <stdio.h>
int main()
{
    int L1, L2, L3, ang, e;
    printf("Entre com o primeiro lado do triangulo: ");
    scanf("%d", &L1);
    printf("Entre com o segundo lado do triangulo: ");
    scanf("%d", &L2);
    printf("Entre com o angulo (em radianos) que L1 e L2 formam: ");
    scanf("%d", ang);

    e = sqrt(pow(L1))



    return 0;
}
